<Query Kind="Program" />

void Main()
{
	var isAdmin= false;
	string reporter;

	reporter = isAdmin ?"Lisa": "sam"; //if (isAdmin) {Lisa} else 	{sam}
	Console.WriteLine(reporter);
	
}
public static string getsomething()
{
	return "sam";
}
// Define other methods and classes here
