<Query Kind="Program" />

void Main()
{
	Console.WriteLine("Beging...."+ DateTime.Now);
	System.Threading.Thread.Sleep(TimeSpan.FromMinutes(1));
	Console.WriteLine("Finish..."+ DateTime.Now);
}

// Define other methods and classes here
